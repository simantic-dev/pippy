[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simantic"
version = "0.1.0"
description = "Python SDK and pytest plugin for the Simantic circuit and firmware simulators"
# PyYAML reads sim-fixtures test.yaml manifests; pythonnet hosts the
# simulation engine (Simantic.Core, .NET) in-process for `simantic.Sim`.
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Simantic" }]
keywords = ["spice", "simulation", "eda", "kicad", "pytest", "firmware", "circuit"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: Pytest",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Electronic Design Automation (EDA)",
]
dependencies = ["pyyaml>=6", "pythonnet>=3.0.5"]

[project.urls]
Homepage = "https://simantic.dev"
Source = "https://github.com/simantic-dev/simantic-py"
Issues = "https://github.com/simantic-dev/simantic-py/issues"

[project.entry-points.pytest11]
simantic = "simantic.pytest_plugin"

[project.scripts]
simantic = "simantic._cli:main"
# `smtc` is the short form, matching the smtc_ token prefix. Same entry point,
# so the two names cannot drift.
smtc = "simantic._cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[tool.hatch.build.targets.wheel]
packages = ["src/simantic"]
